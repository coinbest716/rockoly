// eslint-disable-next-line no-unused-vars
import './graphql/graphql.service.js';

module.exports = function (app) {

  // As this service supports asyn/await so configure service throws error
  // app.configure(graphql);

};
